node_modules/
.next/
.venv/
__pycache__/
.env
.env.local
*.local
qdrant_data/
.playwright-mcp/
playwright-report/
blob-report/
playwright/.cache/
my-video/
*.pptx
*.xlsx
Screenshot*.png
asli-*.png
test-results/
# stray ad-hoc test images at repo root (real fixtures live in vlm-service/test_data + web/public)
/catalog.png
/live_other.png
/live_same.jpeg

# internal build docs (kept local, not in repo)
CLAUDE.md
AGENTS.md
docs/

# local scratch screenshots dropped in the repo root (kept local, not in repo). Root-only on purpose:
# a bare *.png would silently swallow real assets added anywhere else.
/*.png
