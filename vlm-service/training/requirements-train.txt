# Cloud-GPU / CI training env ONLY — NOT installed in the deployed CPU vlm-service.
# Agent 2: dataset versioning, grade-slope fit + Hub push, DeepFashion2 landmark fine-tune.
# Agent 1: fine-tune the garment same-instance matcher (training/train_garment_embed.py).
datasets>=2.19
huggingface_hub>=0.23
transformers>=4.41
torch>=2.2
numpy
pyarrow>=15          # stream DeepFashion In-shop parquet row-groups (train_garment_embed)
scikit-learn>=1.3    # ROC-AUC eval of the fine-tuned matcher
pillow               # image decode/preprocess (matches the serving preprocessing)
onnx>=1.16           # torch.onnx export of the fine-tuned backbone → served by garment_embed.py
onnxscript           # required by torch>=2.x onnx export path (Colab); train script also forces dynamo=False
